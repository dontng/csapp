程序结构

    局部变量
        默认性质：
                自动存储期限 (局部变量的存储单元是随函数调用时自动分配，函数返回时收回分配)
                块作用域

        静态局部变量 (放置static可以使变量具有静态存储期限，拥有永久的存储单元)
                静态局部变量始终具有块作用域，所以对其他函数是「不可见的」

        形式参数 (同局部变量拥有一样的性质，即自动存储期限和块作用域)
                与局部变量的区别是，每次调用时会对形参自动进行初始化数据

    外部变量